## Description

`report` shapes index results for a human or for a machine. It reads the index
and nothing else: no source adapter, no live repository.

Every report has a table form and a JSON form of the same values, keeping
`--format json` from showing more or less than the table did.
